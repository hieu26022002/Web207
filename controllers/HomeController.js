var HomeController = function ($scope) {
    // $scope(phạm vi) đóng vai trò tương tác qua lại giữa JS và HTML thông qua DOM(Document Object Model)
    // $scope.name là gán cho phạm vi này 1 cái tên là name có giá trị là Nguyễn Văn A.

    $scope.name = 'Nguyễn Văn A';
    $scope.id = 'PH12345';
    $scope.age = 21
}
